@model string
<div class="loader loader-sm" id="@Model">
    <div class="fa-3x loading-spinner">
        <i class="fas fa-circle-notch fa-spin"></i>
    </div>
    <span class="loading-text">Loading</span>
</div>